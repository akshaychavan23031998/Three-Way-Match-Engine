{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci --include=dev --prefix ../..",
  "buildCommand": "npm run vercel-build:api --prefix ../..",
  "functions": {
    "index.ts": {
      "includeFiles": "../../node_modules/swagger-ui-dist/**",
      "maxDuration": 60
    }
  }
}
